This directory contains release files of the ${project.name}
project which is licensed under the Apache Software License, Version 2.0.

The provided files are:
  - ${project.artifactId}-${project.version}-src.(zip|tar.gz):
    Complete source distribution for inspection and custom compilation.
  - ${project.artifactId}-${project.version}-bin.(zip|tar.gz):
    Binary distribution which includes all release artifacts and
    the entire site.